apply_requirements:
  checks:
    - tag_query: ""
      approved:
        enabled: false

workflows:
  - tag_query: "'environments/dev/ap-south-1/s3/**' in dir"
    # Indent the engine block here so it applies to this specific workflow
    engine:
      name: terraform
      version: 1.15.0
    plan:
      - type: init
      - type: plan

# Keep it here as a global default for any other directories, if desired
engine:
  name: terraform
  version: 1.15.0
