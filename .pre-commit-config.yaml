repos:
  # ---------------------------------------------------------------------------
  # General repository hygiene
  # ---------------------------------------------------------------------------
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-added-large-files
        args:
          - --maxkb=1024

      - id: check-merge-conflict

      - id: check-yaml

      - id: check-json

      - id: end-of-file-fixer

      - id: trailing-whitespace

      - id: mixed-line-ending
        args:
          - --fix=lf

      - id: detect-private-key

  # ---------------------------------------------------------------------------
  # Secret scanning
  # ---------------------------------------------------------------------------
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks

  # ---------------------------------------------------------------------------
  # Terraform - native Windows executables
  # ---------------------------------------------------------------------------
  - repo: local
    hooks:
      - id: terraform-fmt
        name: Terraform Format
        entry: terraform
        language: system
        args:
          - fmt
          - -check
          - -recursive
        files: \.tf$

      - id: terraform-validate-shared
        name: Terraform Validate - Shared
        entry: terraform
        language: system
        pass_filenames: false
        args:
          - -chdir=infrastructure/shared
          - validate
        files: ^infrastructure/shared/.*\.tf$

      - id: terraform-validate-dev
        name: Terraform Validate - Dev
        entry: terraform
        language: system
        pass_filenames: false
        args:
          - -chdir=environments/dev
          - validate
        files: ^environments/dev/.*\.tf$

      - id: terraform-validate-staging
        name: Terraform Validate - Staging
        entry: terraform
        language: system
        pass_filenames: false
        args:
          - -chdir=environments/staging
          - validate
        files: ^environments/staging/.*\.tf$

      - id: terraform-validate-prod
        name: Terraform Validate - Prod
        entry: terraform
        language: system
        pass_filenames: false
        args:
          - -chdir=environments/prod
          - validate
        files: ^environments/prod/.*\.tf$

      - id: tflint
        name: TFLint
        entry: tflint
        language: system
        pass_filenames: false
        args:
          - --chdir=infrastructure/shared
          - --format=compact
        files: \.tf$

      - id: checkov
        name: Checkov
        entry: checkov
        language: system
        pass_filenames: false
        args:
          - -d
          - .
          - --framework
          - terraform
        files: \.tf$
